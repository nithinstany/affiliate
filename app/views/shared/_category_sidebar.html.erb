<div class="leftContent left">
<ul><% Category.all.each do |category| %>
      <% unless category.listings.featured.blank? %>
         <li> <%= link_to category.name, category_listings_path(category) %> </li>
      <% end %>
<%end%>
</ul>
</div>

