<%= form_for [:admin,@affiliate_merchant], :html => { :multipart => true }  do |f| %>
  <% if @affiliate_merchant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@affiliate_merchant.errors.count, "error") %> prohibited this affiliate_merchant from being saved:</h2>

      <ul>
      <% @affiliate_merchant.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
